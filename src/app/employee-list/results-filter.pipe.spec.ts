import { ResultsFilterPipe } from './results-filter.pipe';

describe('ResultsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
